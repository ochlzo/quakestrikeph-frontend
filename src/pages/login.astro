---
import Layout from "../layouts/Layout.astro";
import { LoginPage } from "../components/login-page";

const redirectTo = Astro.url.searchParams.get("redirectTo") ?? "/dashboard";
---

<Layout>
  <LoginPage client:load redirectTo={redirectTo} />
</Layout>
